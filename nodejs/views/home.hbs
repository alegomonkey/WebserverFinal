<!DOCTYPE html>
<html>
<head>
    <title>Session Demo</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    {{> nav user=user }}

    <div class="content-box">
        <h1>The Outlaw’s Ledger</h1>
        
        <h2>Current Partner Information</h2>
        <p><strong>Name:</strong> {{user.name}}</p>
        <p><strong>Status:</strong> {{#if user.isLoggedIn}}Checked In at the Saloon{{else}}Guest{{/if}}</p>
        {{#if user.isLoggedIn}}
        <p><strong>Check In Time:</strong> {{user.loginTime}}</p>
        <p><strong>Visit Count:</strong> {{user.visitCount}}</p>
        {{/if}}
        
        <p>
            {{#if user.isLoggedIn}}
                <a href="/profile">View Wanted Poster</a> | 
                <form method="POST" action="/logout" style="display: inline;">
                    <button type="submit">Hit the Trail</button>
                </form>
            {{else}}
                <p>New Visitors must Sign The Ledger before checking in at the Saloon.</p>
                <a href="/login">Check In at the Saloon</a>
            {{/if}}
        </p>
    </div>
    {{> footer }}
</body>
</html>